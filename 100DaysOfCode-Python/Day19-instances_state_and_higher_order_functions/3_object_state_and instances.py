# What if we require multiple turtles at same time.???
# and how do we get hold of them.?


# We can create as many as we want in same manner as we were doing all along.
from turtle import Turtle


tim = Turtle()
tommy = Turtle()
tom = Turtle()
jerry = Turtle()

# these all the the turtle objects created from Turtle class.
# Technically these r each a seperate instance i.e., an example of turtle object.


# STATE
# these instance can have diff. attributes and can be performing diff. methods at any one time.
# this is called as STATE or precisely object's state.


# Basically multiple objects r called as Instance
# State: at any time, diff objects can be doing diff things such as using a variable or runing a function --- their state can be diff.