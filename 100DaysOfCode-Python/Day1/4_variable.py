# instead of doing everything in one line, we can store the value in a variable and then use it later in the program.

print("Hello " + input("Enter your name: "))

name = input("Enter your name: ")
print("Hello " + name + "!")